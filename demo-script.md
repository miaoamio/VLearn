# Git 练习厨房 -- 10分钟单人演示剧本

> 面向设计师 / 非技术人员的 Git 入门讲解，用「做汉堡」类比贯穿全程。

---

## 开场白（0:00 - 0:40）

大家好，今天我用一个"做汉堡"的小游戏，带你们 10 分钟搞懂 Git 到底在干嘛。

Git 的本质就四个字：**版本管理**。想象你在一家汉堡店当厨师——你不断试新配方，有时候翻车想退回去，有时候同事也在改配方，你们得协作。Git 就是帮你搞定这些事的工具。

好，打开我们的「Git 练习厨房」。你现在看到的界面分三块：顶部是操作按钮，左边是你的工作台（能看到当前汉堡配方），右边是版本节点树（每次存档会多一个点）。

---

## 第一步：克隆仓库 -- 把店里的菜谱拉到自己工位（0:40 - 1:30）

现在右边显示"远端仓库已建好"——这就好比总店已经有一份基础菜谱放在云端（GitHub），你新入职，第一件事是把菜谱拷一份到自己的工位。

**【操作：点击顶部「克隆 clone」按钮】**

好，clone 成功。左边面板出现了"当前分支 main"，工作区是空的——因为基础菜谱已经加载好了，右边提示"还没有任何存档"。这就是你本地的厨房，可以开始干活了。

> 一句话总结：clone = 从云端把项目拉到本地。

---

## 第二步：加食材 -- 用 AI 帮厨组装汉堡（1:30 - 2:40）

现在我们来做汉堡。左边面板"工作区汉堡"那里，上下两片面包之间是空的，我们往里面加食材。

**【操作：点击「🤖 让 AI 加一层」按钮】**

弹出了一个食材选择器——西红柿、生菜、牛肉、芝士。

**【操作：选择 🥬 生菜】**

看，汉堡里多了一层生菜。再加一层。

**【操作：再次点击「🤖 让 AI 加一层」，选择 🥩 牛肉】**

现在汉堡有两层了：生菜和牛肉。注意左边"未存档的修改"区域变黄了，显示"+ 第1层 新增 🥬 生菜"、"+ 第2层 新增 🥩 牛肉"——这就是 Git 里的 diff，告诉你工作区和上次存档之间有什么差异。

我还可以让 AI 改某一层。

**【操作：点击牛肉那层右边的 🤖 按钮，选择 🧀 芝士】**

牛肉变成了芝士。diff 区会实时更新。

> 一句话总结：工作区 = 你正在编辑的文件，还没保存到 Git 历史里。

---

## 第三步：存档 -- 把当前配方拍个快照（2:40 - 3:30）

改得差不多了，该存一下。存档在 Git 里叫 commit。

**【操作：点击顶部「存档 commit」按钮】**

弹出对话框让你写描述——就像给这版配方取个名字。

**【操作：输入"加了生菜和芝士"，点确定】**

搞定！右边节点树冒出了第一个绿色圆点，旁边写着"绿1: 加了生菜和芝士"。左边的 diff 区变回了"工作区干净"——因为工作区和最新存档一致了。

现在我再加一层西红柿，再存一次档。

**【操作：加一层 🍅 西红柿，然后存档，输入"加了西红柿"】**

节点树多了第二个点。两个点之间有连线——这就是你的版本历史，从下往上是时间线。鼠标悬浮到节点上试试。

**【操作：鼠标 hover 到第一个节点】**

弹出详情卡片，显示那个版本的汉堡长什么样，绿色标记是新增的层。

> 一句话总结：commit = 给当前状态拍快照，随时可以回来找。

---

## 第四步：推送到远端 -- 备份 + 共享（3:30 - 4:10）

现在这两个存档只在你本地。如果电脑坏了就没了。我们把它推送到远端（云端仓库）。

**【操作：点击「同步 sync」按钮（此时按钮上应显示 ↑2）】**

注意按钮上的 "↑2" 表示你有 2 个提交待推送。点一下——push 成功。

看左边"同步状态"变成了"已同步"。右边节点树的圆点从空心变成了实心——实心表示本地和远端都有这个版本，空心/虚线边框表示只存在于一端。

> 一句话总结：push = 把本地存档上传到云端，sync 按钮会自动判断先拉还是先推。

---

## 第五步：切到小红视角 -- 模拟同事协作（4:10 - 5:20）

Git 的核心场景是多人协作。现在我切到"小红"视角，模拟另一个厨师在同一个项目上干活。

**【操作：点击右上角「小红」按钮】**

界面顶部变成了红色调。左边面板显示的是小红的工作区。小红也有同样的汉堡配方（因为她之前也 clone 了）。

我用小红做一些修改——比如把生菜改成牛肉。

**【操作：点击生菜层的 🤖 按钮，选择 🥩 牛肉】**

然后小红存档。

**【操作：点击「存档 commit」，输入"换成牛肉"，确定】**

小红也存好了。现在小红要把改动推到远端。

**【操作：点击「同步 sync」按钮】**

推送成功。现在远端仓库里同时有小绿和小红的提交了。

---

## 第六步：切回小绿，拉取小红的改动（5:20 - 6:10）

**【操作：点击「小绿」按钮切回来】**

注意同步按钮变成了 "↓1"——表示远端有 1 个新提交是你本地没有的（就是小红刚推的那个）。

**【操作：点击「同步 sync」按钮】**

拉取成功！如果小红改的位置和你不冲突，Git 会自动合并。但如果你们改了同一层食材——就会出现**冲突**。

我来演示冲突的情况。先让小绿也改一下汉堡，改同一层为芝士，存档并推送。

（如果此时产生冲突，会弹出冲突解决对话框。）

冲突对话框会展示两边的汉堡——左边是小绿的版本，右边是小红的版本，冲突的那层会标红。你有三个选择：保留小绿的、保留小红的、或者合并两者。

**【操作：选择「合并两者」】**

冲突解决完毕，节点树会出现一个合并节点（有两条线汇入的点）。

> 一句话总结：冲突 = 两人改了同一个地方，Git 不知道听谁的，需要人工决定。

---

## 第七步：AI 翻车率 -- 模拟 AI 犯错（6:10 - 7:10）

现在演示一个有趣的功能。右上角有个"翻车率"滑块，默认 0%。

**【操作：把 AI 翻车率滑块拖到 50%】**

这模拟的是什么呢？当你用 AI 辅助写代码时，AI 有时候会"顺手"改坏别的地方——我们叫它"AI 幻觉"。

**【操作：点击某一层的 🤖 按钮修改食材】**

看！AI 帮你改好了那一层，但另一层变成了 🤢（AI 幻觉物）——这就是翻车了。在真实场景里，就是 AI 改了你没让它改的代码。

这时候你就需要 review 每次 AI 的改动，如果发现翻车了——别慌，我们回退。

> 一句话总结：翻车率滑块帮你体验"为什么要 review AI 的输出"，以及为什么版本管理很重要。

---

## 第八步：回退 -- 时光倒流到安全版本（7:10 - 7:50）

AI 翻车了，汉堡里有个恶心的食材。我不想要这个版本了。

**【操作：点击顶部「回退 reset」按钮】**

弹出一个列表，显示你所有的存档历史。每个选项旁边还能看到当时汉堡的配方。

**【操作：选择上一个正常的存档】**

嘣，回到了翻车之前的状态。右边节点树上，回退掉的那个节点会变成虚化的（不可达状态）。工作区恢复了干净的配方。

> 一句话总结：reset = 回到过去某个版本，后面的存档就不要了。这就是为什么频繁 commit 很重要——给自己留更多"存档点"。

---

## 第九步：开分支 -- 试验新配方不影响主菜单（7:50 - 8:50）

现在我想试一个大胆的新配方，但不想影响正在卖的版本。这就是 branch 的用途。

**【操作：点击「开分支 branch」按钮，输入"new-recipe"，确定】**

左边面板显示当前分支变成了"new-recipe"（颜色不同），右边节点树的分支标签也更新了。

在这个新分支上，我随便折腾。

**【操作：删掉一层、加两层新食材、存档】**

搞定。现在我切回 main 看看。

**【操作：点击左边 branch 列表里的"main"标签切换回去】**

汉堡恢复了！分支上的改动完全没有影响 main。这就是分支的意义——隔离实验。

如果新配方试验成功了，我就把它合并回来。

**【操作：点击「合并 merge」按钮，选择 new-recipe】**

合并完成！main 分支拥有了新配方的内容。节点树上可以看到两条线汇合成一条。

> 一句话总结：branch = 开一条平行世界线去试验，成功了再 merge 回主线。

---

## 第十步：发布 -- 从厨房到门店（8:50 - 9:50）

配方定好了，该上菜了。点击「发布」按钮。

**【操作：点击顶部「发布」按钮】**

发布面板告诉你，当前远端 main 最新版本会被发布。有两个选项：

1. **试吃发布（小流量）**——只让一部分顾客先尝尝
2. **全店发布（全量）**——所有人都能点到

先小范围试试。

**【操作：选择「试吃发布（小流量）」】**

左边"门店状态"面板更新了：试吃版本上线。节点树上对应的节点也多了一个"试吃"标签。

如果试吃反馈 OK：

**【操作：再次点击「发布」，选择「试吃通过 → 全店供应」】**

全店发布成功！如果试吃发现有问题（比如之前 AI 翻车的版本不小心发出去了），还可以一键回滚。

注意：如果你发布了一个包含 AI 幻觉食材的版本，门店状态会显示"客诉！"——这就是为什么发布前要做好 review 和测试。

> 一句话总结：发布 = 代码从仓库到生产环境的最后一步。试吃=灰度发布，全店=全量上线，出问题可以回滚。

---

## 收尾总结（9:50 - 10:00）

回顾一下我们刚才走过的完整流程：

| 步骤 | Git 操作 | 汉堡比喻 |
|------|----------|-----------|
| 克隆 | clone | 从总店拿到菜谱 |
| 编辑 | 工作区改动 | 在工位上调配方 |
| 存档 | commit | 拍照记录这版配方 |
| 推送 | push/sync | 把配方传回总店 |
| 协作 | pull + 冲突解决 | 跟同事同步进度 |
| 回退 | reset | 翻车了回到上个版本 |
| 分支 | branch + merge | 开辟试验区，成功后合并 |
| 发布 | deploy | 从厨房端上餐桌 |

Git 的核心就这些。剩下的都是这些基础动作的组合。以后你看到设计稿版本管理、Figma 分支，底层逻辑是完全一样的。

谢谢大家，欢迎自己上手玩一下这个练习厨房！
